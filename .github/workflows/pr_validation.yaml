name: PR Version Validation (Prod, Master)
on:
  pull_request: {}

jobs:
  validate:
    uses: mkaraki/mkarakiapps-releasing-actions/.github/workflows/pr-version-validator.yaml@748ffc7eae68411b3ee4fc8b98c22afbd991057a
    with:
      target-branch: ${{ github.base_ref }}
      version-file: version.go
      version-file-format: php
