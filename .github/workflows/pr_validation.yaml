name: PR Version Validation (Prod, Latest)
on:
  pull_request:

jobs:
  validate:
    uses: mkaraki/mkarakiapps-releasing-actions/.github/workflows/pr-version-validator.yaml
    with:
      target-branch: ${{ github.base_ref }}
      version-file: version.go
      version-file-format: php
